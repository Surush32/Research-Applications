# 7-Day Progress To-Do

1. Set up Azure Content Safety (endpoint + key) — done
2. Test Azure Protected Material API with one `.py` file — done
3. Add Lineage `/api/azure-check` route + env vars — done
4. Connect GitHub import (1–20 files) to Azure check — done
5. Show Azure results (detected, citations, license) — done
6. Add recommendations + Azure section in download report — done
7. Scaffold VS Code extension — done (`vscode-extension/`)
8. Add VS Code commands (compare / check / clipboard) — done
9. Connect VS Code extension to shared AST API — done
10. Prepare short instructor demo (web Azure + VS Code once)
